# procedures

En fil per domän (`matches.ts`, `teams.ts`, `participants.ts` …). Varje fil exporterar
procedurer byggda med `implement(contract)`, och `src/router.ts` binder ihop dem till
kontraktets form.

Mappen är tom så länge kontraktet är tomt — endpoints landar i #13–#18.
